/*
use crate::prelude::*;
mod input;

pub enum Command {
    None,
    PlaceTile(Point),
}
pub fn run_systems(state: &mut EditorState) {
    let mut command = Command::None;
    //player input function
    input::parse_input(state, &mut command);
    //player input function
    //map editor
}
*/
